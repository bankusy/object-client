import styled from "styled-components";

export default function Home() {
    return <Container>Terminate</Container>;
}

const Container = styled.div`
    width: 100%;
    height: 100vh;
    background-color: var(--background-color);
`;
