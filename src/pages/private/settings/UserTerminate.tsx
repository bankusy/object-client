import styled from "styled-components";

export default function UserTerminate() {
    return <TerminateContainer>Terminate</TerminateContainer>;
}

const TerminateContainer = styled.div`
    width: 100%;
    height: 100vh;
    background-color: var(--background-color);
`;
